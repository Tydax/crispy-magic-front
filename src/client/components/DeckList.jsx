import React, { PropTypes } from 'react';

const DeckList = function DeckList({ userDecks }) {
  return (
    <div className='deck-list-page'>
      <h1>Mes decks</h1>
    </div>
  );
};

export default DeckList;
